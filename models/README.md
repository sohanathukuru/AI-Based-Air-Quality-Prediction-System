# Trained Models

The trained model files are not included in this repository because they exceed GitHub's file size limitations.

## Models Generated

- random_forest_model.pkl
- lstm_model.keras

These models can be regenerated by running the training scripts available in the `src` directory.

Training Pipeline:

1. Data Preprocessing
2. Feature Engineering
3. Random Forest Training
4. LSTM Training

The generated model files will be saved automatically in the `models` directory.
